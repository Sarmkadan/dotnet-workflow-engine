public static class DependencyInjection
		// Add argument null guards to public methods in this class
		// For each parameter that is string type: add ArgumentException.ThrowIfNullOrEmpty(paramName) as first line.
		// For each parameter that is a reference type (not string, not value type): add ArgumentNullException.ThrowIfNull(paramName).
		// Skip parameters that are already guarded, or that have default value null.
		// Run: dotnet build. Must compile. Commit.

		public static class DependencyInjection
